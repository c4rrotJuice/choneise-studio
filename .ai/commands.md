# Project Commands Cache

Generated routing cache for agents. Verify commands against package and tool config before relying on them.

Run `.continue/skim-cache.sh` from the repository root to refresh this cache.
